 
let money=400;
let res=money>500 ? 'biriyani' : 'nothing';

let res1=(money>500 && money<400) ?  'biriyani' : 'nothing';

//function using ternary
const isActive=true;
const showUSer=()=>console.log('display');
const hideUSer=()=>console.log('hide');
isActive ? showUSer() : hideUSer();

//convert in string
const n=50;
const str=n+'';


//convert string to number
const input='500';
const inNum=  +input;
